import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryColor = Color(0xFF3C2117);

  // Background Colors
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // Card & Container Colors
  static const Color primaryCardColor = Color(0xFFE9D6AA);
  static const Color primaryCard10Color = Color(0xFFFFF4D9);

  // Dark Mode Surfaces (layered: Background < Surface < Card < Input < Border)
  // Warm Autumn palette: browns, caramel, cream, warm charcoal
  static const Color darkBackground = Color(0xFF0F0C08);
  static const Color darkSurface = Color(0xFF241C12);
  static const Color darkSurfaceStrong = Color(0xFF5D4C38);
  static const Color darkCard = Color(0xFF241C12);
  static const Color darkInputFill = Color(0xFF675544);

  // Button Colors
  static const Color secondaryGold = Color(0xFF735A12);

  // Text Colors
  static const Color textPrimary = Color(0xFF2F1A19);
  static const Color textSecondary = Color(0xFF767373);

  // Dark Mode Text Colors
  static const Color darkTextPrimary = Color(0xFFF0E4DC);
  static const Color darkTextSecondary = Color(0xFFA89A93);

  // Additional colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Dark Mode Borders
  // Subtle warm-brown borders, no purple/mauve/cold gray
  static const Color darkBorder = Color(0xFF362918);
}
